name: lint
on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: "structurizr/cli"
    steps:
      - uses: actions/checkout@v3
      - run: chmod +x /usr/local/structurizr-cli/structurizr.sh
      - name: validate omit
        run: |
          ls
          /usr/local/structurizr-cli/structurizr.sh validate omit.dsl
